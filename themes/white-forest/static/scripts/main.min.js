function init() {
    "use strict";
    window.addEventListener("scroll", function() {
        var e = window.pageYOffset || document.documentElement.scrollTop,
            s = 100,
            i = document.querySelector(".single-heading"),
            t = document.querySelector(".sidebar"),
            home = document.querySelector(".single-heading svg");
        i && (e > s ? (i.classList.add("smaller"), t.classList.add("sidebar1"), t.classList.add("g-medium--half")) : (i.classList.remove("smaller"), t.classList.remove("sidebar1")))
    })
}! function() {
    "use strict"
}(), window.onload = init();
